## AI-Assisted Development Log
# Art Explorer - CSCI 571 Assignment 3
# Student Name: Khang Thai
# Date: May 4, 2026

================================================================================
SESSION 1: Project Setup and Backend Skeleton
================================================================================

PROMPT 1:
"Help me get started with this Art Collection Explorer app."

AI RESPONSE SUMMARY:
- Reviewed the required project structure and assignment specification
- Recommended a Node.js/Express backend with a static frontend
- Suggested creating the main files: server.js, package.json, public/index.html, 
CSS, and JS modules

WHAT I LEARNED:
- Express can serve both API endpoints and static frontend files
- A clean file structure makes it easier to separate backend and frontend logic
- Starting with a minimal working backend is better than trying to build 
everything at once

ISSUES ENCOUNTERED:
- Key files were empty at the start

HOW I FIXED IT:
- Created the basic app structure and started with the backend rout layout and
frontend skeleton

--------------------------------------------------------------------------------

PROMPT 2:
"What should the backend API endpoints look like for searching Met, Harvard, and 
Wikipedia?"

AI RESPONSE SUMMARY:
- Help define route for search, artwork details, artist biography, and related works
- Suggested normalizing data from different museum APIs into one shared format
- Recommended using a Harvard API key on the server only

WHAT I LEARNED:
- Multiple APIs can be combined if they are normalized into one object shape
- The backend should hire API keys and proxy external requests
- Server-side pagination is easier when the backend returns a consistent response format

--------------------------------------------------------------------------------

PROMPT 3:
"How do I make the search results update while typing and support filtering?"

AI RESPONSE SUMMARY:
- Suggested debounced search-as-you-type with a search delay
- Showed how to pass query, page, source, and image filter values to the backend
- Recommended keeping search state in the frontend

WHAT I LEARNED:
- Debouncing prevents too many API calls while the user types
- UI state should stay in sync with the URL parameters and active filters
- Search results should be updated without refreshing the whole page

================================================================================
SESSION 2: UI Layout, Cards, and Detail View
================================================================================

Prompt 4:
"How can I make the app look like the reference screenshots with Bootstap 5?"

AI RESPONSE SUMMARY:
- Recommended a dark navbar, centered search panel, and responsive result cards 
- Suggested using Bootstrap grid classes for desktop/tablet/mobile layouts
- Helped style the cards with badges, shadows, rounded corners, and consistent spacing

WHAT I LEARNED:
- Bootstrap layout classes can produce a polished responsive design quickly
- Visual hierarchy matters: title, artist, date, and image should have clear separation
- Small style choices like shadows and spacing make the UI feel much cleaner

ISSUES ENCOUNTERED:
- The layout looked too plain at first
- Some elements were too close together on smaller screens 

HOW I FIXED IT:
- Added custom CSS for spacing, image sizing, card shadows, and repsonseive adjustments

--------------------------------------------------------------------------------

PROMPT 5:
"How do I build a tabbed detail view with overview, biography, related works, and map?"

AI RESPONSE SUMMARY:
- Suggested using Bootstrap tabs for the detail panel
- Recommended loading artist biography from Wikipedia only when the tab opens
- Showed how to render a Leaflet map for the museum location tab

WHAT I LEARNED:
- Bootstap tabs are a good fit for switching detail sections cleanly
- Lazy-loading tab content keeps the app faster and simpler
- Leaflet can be used with fixed museum coordinates to display location maps

--------------------------------------------------------------------------------

PROMPT 6:
"How should I store and manage favorites in the browser?"

AI RESPONSE SUMMARY:
- Recommended localStorage for persistance across favorites
- Suggested helper functions for add, remove, toggle, and render favorites
- Encouraged using a consisten sourced:id key for uniqueness

WHAT I LEARNED:
- localStorage is a simple way to persist user data without a backend database
- Favorites need a stable unique identifier
- Re-rendering the favorites grid after each change keeps the UI accurate

================================================================================
SESSION 3: Debugging API Errors and Search Behavior
================================================================================

PROMPT 7:
"I'm getting errors when I search. How do I debug why the app says all sources
failed?"

AI RESPONSE SUMMARY:
- Help inspect backend and frontend error handling
- Suggested checking the exact responsive from /api/search
- Recommended verifying whether the server was runnin and whether Met or Harvard
were failing independently

WHAT I LEARNED:
- Browser errors often come from the backend response body, not just the UI
- Partial failure handling is better than failing the whole search when only one source breaks
- DevTools Network can show the exact request and response

ISSUES ENCOUNTERED:
- The app sometimes returned 502 errors from a failed upstream API
- Some requests were hitting an old or stopped server process

HOW I FIXED IT:
- Improved backend error handling and confirmed the correct server process was running
- Used terminal checks like curl and lsof to verify the server and API endpoints

--------------------------------------------------------------------------------

PROMPT 8:
"Why am I seeing no-image results when the Has Image checkbox is checked?"

AI RESPONSE SUMMARY:
- Helped tighten the server-side filtering logic
- Suggested checking the final combined result set, not just source-specific filtering
- Recommended making the filter use real non-empty image URLs

WHAT I LEARNED:
- The final filter should happen after combining results from both APIs
- Some API records may contain empty or inconsistent image fields
- The frontend label and backend behavior need to match exactly

HOW I FIXED IT:
- Updated the backend to filter out any result without a real image URL when the checkbox is checked
- Kept unchecked behavior as "show all results"

--------------------------------------------------------------------------------

PROMPT 9:
"How do I make the server more reliable when APIs return 403 or rate limit errors?"

AI RESPONSE SUMMARY:
- Recommended addng a User-Agent header to outbound requests
- Suggested retry logic for temporary failures
- Encouraged keeping Met and Harvard failures isolated so one source doesn't kill the whole search

HOW I FIXED IT:
- Added headers and retries in server-side fetch logic
- Improved error reporting

================================================================================
REFLECTION
================================================================================

Most useful AI assistance:
- Designing the backend route structure
- Understanding how to normalize data from Met, Harvard, and Wikipedia
- Debugging API failures and filter behavior
- Building the Bootstrap detail view and responsive card layout

What I would do differently:
- Test each feature in smaller steps before layering on more behavior
- Be more careful about matching frontend labels with backend filter logic

Time spend: ~15 hours
AI tool used: Claude, Codex
